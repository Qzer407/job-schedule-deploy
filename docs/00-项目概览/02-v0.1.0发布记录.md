# v0.1.0 发布记录

## 发布信息

| 属性 | 内容 |
|------|------|
| 版本号 | v0.1.0 |
| 版本名称 | MVP基础版 |
| 发布时间 | 2026-05-13 |
| 发布人 | - |

---

## 一、功能清单

| 功能模块 | 功能点 | 说明 | 状态 |
|---------|--------|------|------|
| 任务管理 | 任务CRUD | 任务的增删改查 | ✅ |
| 任务管理 | 任务启动/停止 | 启动和停止任务调度 | ✅ |
| 任务管理 | 任务触发 | 手动触发任务执行 | ✅ |
| 监控中心 | 执行统计 | 任务执行成功率统计 | ✅ |
| 监控中心 | 任务日志 | 任务执行日志查询 | ✅ |

---

## 二、后端目录结构

### 2.1 迭代前结构

```
backend/
├── src/
│   └── main/
│       ├── java/
│       │   └── com/
│       │       └── qzer/
│       │           └── scheduler/
│       │               └── TaskSchedulerApplication.java
│       └── resources/
│           └── application.yml
├── pom.xml
└── Dockerfile
```

### 2.2 迭代后结构

```
backend/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/
│   │   │       └── qzer/
│   │   │           └── scheduler/
│   │   │               ├── TaskSchedulerApplication.java
│   │   │               ├── controller/
│   │   │               │   ├── TaskController.java
│   │   │               │   └── MonitorController.java
│   │   │               ├── service/
│   │   │               │   ├── TaskService.java
│   │   │               │   ├── TaskServiceImpl.java
│   │   │               │   ├── MonitorService.java
│   │   │               │   └── MonitorServiceImpl.java
│   │   │               ├── entity/
│   │   │               │   ├── TaskInfo.java
│   │   │               │   └── TaskLog.java
│   │   │               ├── mapper/
│   │   │               │   ├── TaskInfoMapper.java
│   │   │               │   └── TaskLogMapper.java
│   │   │               ├── dto/
│   │   │               │   ├── request/
│   │   │               │   │   ├── TaskCreateRequest.java
│   │   │               │   │   └── TaskUpdateRequest.java
│   │   │               │   └── response/
│   │   │               │       └── ApiResponse.java
│   │   │               └── config/
│   │   │                   └── XxlJobConfig.java
│   │   └── resources/
│   │       └── application.yml
│   └── test/
│       └── java/
│           └── com/
│               └── qzer/
│                   └── scheduler/
│                       └── service/
│                           └── TaskServiceTest.java
├── pom.xml
└── Dockerfile
```

### 2.3 新增文件清单

| 文件路径 | 说明 |
|---------|------|
| `controller/TaskController.java` | 任务管理控制器 |
| `controller/MonitorController.java` | 监控控制器 |
| `service/TaskService.java` | 任务服务接口 |
| `service/TaskServiceImpl.java` | 任务服务实现 |
| `service/MonitorService.java` | 监控服务接口 |
| `service/MonitorServiceImpl.java` | 监控服务实现 |
| `entity/TaskInfo.java` | 任务信息实体 |
| `entity/TaskLog.java` | 任务日志实体 |
| `mapper/TaskInfoMapper.java` | 任务Mapper |
| `mapper/TaskLogMapper.java` | 日志Mapper |
| `dto/request/TaskCreateRequest.java` | 创建任务请求DTO |
| `dto/request/TaskUpdateRequest.java` | 更新任务请求DTO |
| `dto/response/ApiResponse.java` | 统一响应DTO |
| `config/XxlJobConfig.java` | XXL-JOB配置 |
| `test/TaskServiceTest.java` | 任务服务测试 |

---

## 三、前端目录结构

### 3.1 迭代前结构

```
frontend/
├── index.html
├── package.json
└── vite.config.js
```

### 3.2 迭代后结构

```
frontend/
├── src/
│   ├── main.ts
│   ├── App.vue
│   ├── components/
│   │   ├── TaskManager.vue
│   │   └── Dashboard.vue
│   └── api/
│       └── index.js
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
└── tsconfig.node.json
```

### 3.3 新增文件清单

| 文件路径 | 说明 |
|---------|------|
| `src/main.ts` | 入口文件 |
| `src/App.vue` | 根组件 |
| `src/components/TaskManager.vue` | 任务管理组件 |
| `src/components/Dashboard.vue` | 监控仪表盘组件 |
| `src/api/index.ts` | API封装 |
| `tsconfig.json` | TypeScript配置 |
| `tsconfig.node.json` | Node TypeScript配置 |

---

## 四、数据库变更

### 4.1 新增表

```sql
-- 任务信息表
CREATE TABLE IF NOT EXISTS `t_task_info` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `task_name` VARCHAR(200) NOT NULL,
    `task_group` VARCHAR(100) NOT NULL,
    `cron_expression` VARCHAR(100) NOT NULL,
    `executor_handler` VARCHAR(200) NOT NULL,
    `executor_param` VARCHAR(500),
    `sharding_total` INT DEFAULT 1,
    `sharding_param` VARCHAR(500),
    `retry_count` INT DEFAULT 3,
    `alarm_status` TINYINT DEFAULT 1,
    `status` TINYINT DEFAULT 0,
    `description` VARCHAR(500),
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME ON UPDATE CURRENT_TIMESTAMP,
    INDEX `idx_task_group` (`task_group`),
    INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 任务执行日志表
CREATE TABLE IF NOT EXISTS `t_task_log` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `task_id` BIGINT NOT NULL,
    `task_name` VARCHAR(200) NOT NULL,
    `executor_address` VARCHAR(200),
    `sharding_index` INT DEFAULT 0,
    `sharding_param` VARCHAR(500),
    `trigger_time` DATETIME NOT NULL,
    `start_time` DATETIME,
    `end_time` DATETIME,
    `duration` BIGINT,
    `execution_status` TINYINT DEFAULT 0,
    `execution_log` TEXT,
    `error_msg` TEXT,
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX `idx_task_id` (`task_id`),
    INDEX `idx_trigger_time` (`trigger_time`),
    INDEX `idx_execution_status` (`execution_status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 五、接口清单

| 接口名称 | HTTP方法 | 路径 | 说明 |
|---------|----------|------|------|
| 查询任务列表 | GET | /api/v1/tasks | 分页查询任务 |
| 查询任务详情 | GET | /api/v1/tasks/{id} | 查询单个任务 |
| 创建任务 | POST | /api/v1/tasks | 创建新任务 |
| 更新任务 | PUT | /api/v1/tasks/{id} | 更新任务 |
| 删除任务 | DELETE | /api/v1/tasks/{id} | 删除任务 |
| 启动任务 | POST | /api/v1/tasks/{id}/start | 启动任务 |
| 停止任务 | POST | /api/v1/tasks/{id}/stop | 停止任务 |
| 触发任务 | POST | /api/v1/tasks/{id}/trigger | 手动触发 |
| 获取统计 | GET | /api/v1/monitor/statistics | 获取执行统计 |
| 查询日志 | GET | /api/v1/logs | 分页查询日志 |
| 日志详情 | GET | /api/v1/logs/{id} | 查询日志详情 |

---

## 六、技术亮点

1. **基于XXL-JOB**：集成成熟的任务调度平台，支持分布式任务调度
2. **统一响应格式**：所有API返回统一的ApiResponse格式
3. **分页查询**：任务列表和日志查询支持分页
4. **TypeScript支持**：前端已迁移到TypeScript，提升代码质量

---

## 七、已知问题

| 问题 | 严重程度 | 说明 | 状态 |
|------|---------|------|------|
| 无 | - | - | - |

---

## 八、升级说明

### 8.1 环境要求

- JDK 21+
- Node.js 18+
- MySQL 8.0+
- XXL-JOB Admin 2.4.x

### 8.2 升级步骤

1. 执行数据库脚本 `deploy/config/schema.sql`
2. 启动 XXL-JOB Admin
3. 配置执行器
4. 启动后端服务
5. 启动前端服务

---

*发布记录生成时间：2026-05-13*
