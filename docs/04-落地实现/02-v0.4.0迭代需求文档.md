# v0.4.0 - 任务编排迭代需求文档

## 1. 迭代概述

| 属性 | 内容 |
|------|------|
| 迭代版本 | v0.4.0 |
| 迭代名称 | 任务编排 |
| 迭代周期 | 2026-05-13 |
| 迭代负责人 | - |

---

## 2. 需求清单

| 序号 | 功能点 | 优先级 | 预估工时 | 状态 |
|-----|--------|--------|---------|------|
| 1 | 任务依赖配置 | P0 | 8h | ✅ 已完成 |
| 2 | DAG可视化编辑器 | P0 | 16h | ✅ 已完成 |
| 3 | 依赖调度执行 | P0 | 12h | ✅ 已完成 |
| 4 | 子任务管理 | P1 | 8h | ✅ 已完成 |
| 5 | 任务链执行 | P1 | 6h | ✅ 已完成 |
| 6 | 工作流模板 | P2 | 8h | ✅ 已完成 |
| 7 | 前端DAG展示 | P1 | 8h | ✅ 已完成 |

---

## 3. 技术设计

### 3.1 后端设计

**新增文件：**
```
backend/src/main/java/com/qzer/scheduler/
├── controller/
│   ├── SubTaskController.java         # 子任务控制器
│   └── WorkflowTemplateController.java # 工作流模板控制器
├── service/
│   ├── SubTaskService.java            # 子任务服务接口
│   ├── WorkflowTemplateService.java   # 模板服务接口
│   └── impl/
│       ├── SubTaskServiceImpl.java    # 子任务服务实现
│       └── WorkflowTemplateServiceImpl.java # 模板服务实现
├── entity/
│   ├── SubTask.java                   # 子任务实体
│   └── WorkflowTemplate.java          # 工作流模板实体
├── mapper/
│   ├── SubTaskMapper.java             # 子任务Mapper
│   └── WorkflowTemplateMapper.java    # 模板Mapper
└── dto/request/
    ├── SubTaskCreateRequest.java      # 创建子任务请求
    ├── SubTaskUpdateRequest.java      # 更新子任务请求
    ├── WorkflowTemplateCreateRequest.java # 创建模板请求
    └── WorkflowTemplateUpdateRequest.java # 更新模板请求
```

**修改文件：**
```
backend/src/main/java/com/qzer/scheduler/
└── util/
    └── DagTopologySort.java           # DAG拓扑排序工具
```

### 3.2 前端设计

**新增文件：**
```
frontend/src/
├── components/
│   └── WorkflowEditor.vue             # DAG可视化编辑器
└── types/
    └── workflow.ts                    # 工作流类型定义
```

---

## 4. 接口设计

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 查询子任务列表 | GET | /api/v1/subtasks/parent/{parentTaskId} | 查询父任务下的子任务 |
| 创建子任务 | POST | /api/v1/subtasks/parent/{parentTaskId} | 创建子任务 |
| 批量创建子任务 | POST | /api/v1/subtasks/parent/{parentTaskId}/batch | 批量创建 |
| 更新子任务 | PUT | /api/v1/subtasks/{id} | 更新子任务 |
| 删除子任务 | DELETE | /api/v1/subtasks/{id} | 删除子任务 |
| 执行子任务 | POST | /api/v1/subtasks/parent/{parentTaskId}/execute | 执行子任务 |
| 查询模板列表 | GET | /api/v1/workflow-templates | 查询模板列表 |
| 创建模板 | POST | /api/v1/workflow-templates | 创建模板 |
| 更新模板 | PUT | /api/v1/workflow-templates/{id} | 更新模板 |
| 删除模板 | DELETE | /api/v1/workflow-templates/{id} | 删除模板 |
| 从模板创建工作流 | POST | /api/v1/workflow-templates/{id}/apply | 应用模板 |

---

## 5. 数据库变更

```sql
-- 子任务表
CREATE TABLE IF NOT EXISTS `t_sub_task` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `parent_task_id` BIGINT NOT NULL,
    `sub_task_name` VARCHAR(200) NOT NULL,
    `executor_handler` VARCHAR(200) NOT NULL,
    `executor_param` VARCHAR(500),
    `retry_count` INT DEFAULT 3,
    `order_index` INT DEFAULT 0,
    `status` TINYINT DEFAULT 1,
    `description` VARCHAR(500),
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME ON UPDATE CURRENT_TIMESTAMP,
    INDEX `idx_parent_task_id` (`parent_task_id`),
    CONSTRAINT `fk_sub_task_parent_task_id` FOREIGN KEY (`parent_task_id`) REFERENCES `t_task_info`(`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 工作流模板表
CREATE TABLE IF NOT EXISTS `t_workflow_template` (
    `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
    `template_name` VARCHAR(200) NOT NULL,
    `template_desc` VARCHAR(500),
    `template_type` VARCHAR(50),
    `template_config` TEXT,
    `status` TINYINT DEFAULT 1,
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME ON UPDATE CURRENT_TIMESTAMP,
    INDEX `idx_template_name` (`template_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 6. 依赖关系

```
任务依赖配置 ──► DAG拓扑排序 ──► 工作流执行器
       │
       └──► 子任务管理 ──► 任务链执行
              │
              └──► 工作流模板
```

---

## 7. 验收标准

| 功能 | 验收标准 | 状态 |
|------|---------|------|
| 子任务管理 | 能够创建、编辑、删除子任务 | ✅ |
| 子任务执行 | 子任务按顺序执行 | ✅ |
| 工作流模板 | 能够创建模板并应用 | ✅ |
| DAG可视化 | 能够可视化展示任务依赖 | ✅ |

---

*文档创建时间：2026-05-13*
